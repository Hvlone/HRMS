<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.RepairReviewMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.po.RepairReview">
        <id column="repair_review_id" jdbcType="INTEGER" property="repairReviewId"/>
        <result column="repair_id" jdbcType="INTEGER" property="repairId"/>
        <result column="tenant_id" jdbcType="INTEGER" property="tenantId"/>
        <result column="rating" jdbcType="INTEGER" property="rating"/>
        <result column="comment" jdbcType="VARCHAR" property="comment"/>
        <result column="review_time" jdbcType="TIMESTAMP" property="reviewTime"/>
    </resultMap>

    <sql id="BaseColumns">
        repair_review_id, repair_id, tenant_id, rating, comment, review_time
    </sql>

    <!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="repairReviewId"
            parameterType="com.example.demo.po.RepairReview">
        INSERT INTO repair_review
            (repair_id, tenant_id, rating, comment, review_time)
        VALUES
            (#{repairId}, #{tenantId}, #{rating}, #{comment}, #{reviewTime})
    </insert>

    <!-- 主键查询 -->
    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM repair_review
        WHERE repair_review_id = #{repairReviewId}
    </select>

    <!-- 查询全部 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM repair_review
        ORDER BY repair_review_id DESC
    </select>

    <!-- 根据维修单id查询 -->
    <select id="selectByRepairId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM repair_review
        WHERE repair_id = #{repairId}
        ORDER BY review_time DESC
    </select>

    <!-- 动态更新 -->
    <update id="updateByPrimaryKeySelective"
            parameterType="com.example.demo.po.RepairReview">
        UPDATE repair_review
        <set>
            <if test="repairId != null">repair_id = #{repairId},</if>
            <if test="tenantId != null">tenant_id = #{tenantId},</if>
            <if test="rating != null">rating = #{rating},</if>
            <if test="comment != null">comment = #{comment},</if>
            <if test="reviewTime != null">review_time = #{reviewTime},</if>
        </set>
        WHERE repair_review_id = #{repairReviewId}
    </update>

    <!-- 主键删除 -->
    <delete id="deleteByPrimaryKey">
        DELETE FROM repair_review WHERE repair_review_id = #{repairReviewId}
    </delete>

</mapper>